# העבר את זה לשיחה החדשה

העתק את כל מה שמתחת לקו, והדבק כהודעה ראשונה בשיחה החדשה.

פתח את השיחה **בתיקייה** `C:\Users\omrit\Desktop\nib` — שם `CLAUDE.md` נטען
אוטומטית וחוסך את רוב ההסבר.

---

אנחנו ממשיכים פרויקט שכבר רץ. התיקייה היא `C:\Users\omrit\Desktop\nib`.

**קרא קודם את `CLAUDE.md` ואת `PROGRESS.md`.** הם מחזיקים את הסכם העבודה, מצב
כל המשימות והמשימה הבאה. אל תשאל אותי מה כבר עשינו — זה כתוב שם.

**שים לב במיוחד לסעיף הראשון ב‑`PROGRESS.md`**, על מדד הסגנון שהתגלה כשבור.
הוא מסייג כמעט כל מספר סגנון שמופיע בקובץ.

## מי אני ואיך לעבוד איתי

אני עמרי, סטודנט שנה שלישית להנדסת נתונים בטכניון. אני הארכיטקט: אני מחליט,
מריץ ומדבג. אתה כותב את רוב הקוד.

- **ענה בעברית.** מונחים טכניים, שמות מודלים וקבצים נשארים באנגלית, בגרשיים
  אחוריים. אל תשלב אנגלית באמצע משפט עברי בלי סימון — זה הופך את הטקסט לבלתי
  קריא בכיווניות מעורבת.
- **הקוד באנגלית** — שמות, הערות, תיעוד. הרפוזיטורי צריך להיקרא טוב.
- **הסבר לפני קוד**, ורכיב אחד בכל פעם.
- **פרק ז'רגון.** הבסיס שלי מוצק — הסתברות, סטטיסטיקה, אלגברה, בסיסי נתונים,
  פייתון. אוצר המילים של deep learning ו‑computer vision פחות.
- **אל תיגע בקבצים מחוץ לתיקייה** בלי אישור לפעולה הספציפית.
- **אל תיתן הערכות זמן שלא מדדת.** בסשן הקודם שלוש הערכות היו שגויות פי
  שלושה עד פי עשרים, כולן מהשלכה ממודל אחד על אחר.
- **בדוק לפני שאתה מחכה.** תהליך שלא מתקדם — בדוק זמן מעבד מול זמן שעון לפני
  שמניחים שהוא עובד.

## מה הפרויקט

`nib` — סינתזת כתב יד few-shot. מקבל עמוד־שניים של כתב יד, מחלץ סגנון, ומייצר
טקסט חדש באותו כתב. **אנגלית בלבד.** המטרה: פרויקט מרכזי בתיק העבודות.

## איפה אנחנו

**שלב 1 הושלם** — 13 משימות תשתית. **שלב 2** — T13 עד T22, המודל מייצר ונמדד.

### שלושת המדדים ומה שהם נותנים על כתב יד אמיתי

```
FID רצפה         19.15      שני חצאים של שורות אמיתיות
CER              11.45%     שיעור הטעות של TrOCR על שורות אמיתיות
HWD              0.641      שתי קבוצות של אותם כותבים
HWD              2.931      אותו טקסט בגופן — אפס סגנון
```

**זיהוי הכותבים כבר לא מדד ראשי.** ראה למטה.

### המספרים של המודלים, וכולם מסויגים

```
                 FID      זיהוי     CER פער    אמינות
Emuru            69.5     22.1%     +20.4      6 כשלים
Eruku            80.7      5.3%     +13.0      0 כשלים
```

**עמודת הזיהוי לא אמינה** ולכן גם ההשוואה ביניהם לא הוכרעה.

## הדבר החשוב ביותר שקרה לאחרונה

**מדד הסגנון שלנו מדד חדות תמונה, לא זהות כותב.**

שורה אמיתית, של הכותב הנכון, בטשטוש 0.8 פיקסל:

```
המדד שלנו        96.8%  →  12.2%      איבד 87%
HWD              0.641  →   0.721     זז 12%
```

טשטוש לא משנה את כתב היד. כל מפענח גנרטיבי מייצר בדיוק את הרכות הזו. כלומר
**Emuru ב‑22.1% מקבל ציון גבוה יותר משורה אמיתית מטושטשת קלות**, וכל השוואת
סגנון שעשינו מבולבלת בחדות הפלט.

`HWD` נכנס כמדד הסגנון הראשי. הוא של אותה קבוצה שבנתה את Emuru ואת Eruku, וזה
מה שהמאמרים שלהם מדווחים — כלומר **המספרים שלנו יהיו ברי‑השוואה לספרות בפעם
הראשונה.** מותקן כתוסף: `pip install -e ".[hwd]"`.

מזהה הכותבים נשאר, **אבל צריך אימון מחדש** עם טשטוש, דגימה חוזרת ורעש
באוגמנטציה. כ‑20 דקות על T4.

## החלטות נעולות

- **מודל מוכן, לא אימון מאפס.** Emuru (CVPR 2025) ו‑Eruku (WACV 2026), שניהם
  zero-shot, רישיונות MIT ו‑Apache-2.0.
- **64 פיקסל.**
- **CVL ולא IAM** — אתר IAM נפל.
- **`transformers` נעול ל‑4.x.** גרסה 5 לא טוענת את Emuru.
- **torch 2.11** כדי להתאים לקולאב.
- **אנגלית בלבד** — הקטע הגרמני (טקסט 6) מוחרג מהחבילה.

## כיוונים שנסגרו — אל תחזור אליהם בלי סיבה

- **`cfg_scale` של Eruku.** ‏1.0 / 1.25 / 2.0 נתנו 10.0% / 5.3% / 6.7%, כל
  הרווחים נחתכים. ‏2.0 גרוע חד‑משמעית. שווה לבחון מחדש רק אחרי ש‑`HWD` יחליף
  את המדד ששפט אותם.
- **ארבע שורות סגנון שוברות את Emuru** (0.25 מהרוחב). שתיים עובדות (0.85).

## המשימה הבאה

**להריץ את `notebooks/colab_eval.ipynb`, תאים 1 עד 7.** זה נותן את מספר
הסגנון האמין הראשון של הפרויקט — `HWD` של Emuru מול רצפה של 0.641.

תא 8 מריץ את Eruku להשוואה, שלוש שעות.

## דברים מעשיים

- הקוד ב‑`github.com/omritzabari/nib`, ציבורי.
- שני נוטבוקים, **אפס לוגיקה** בשניהם: `colab_smoke.ipynb` (תשתית) ו‑
  `colab_eval.ipynb` (הערכה — זה שרלוונטי).
- ב‑Drive: `MyDrive/nib/cvl_lines_64.lmdb` (127 מגה, 9,142 רשומות) ו‑
  `checkpoints/writer_embedder.pt`.
- **מעלים רק מ‑`data/processed/upload/`** — הקובץ ב‑`data/processed/` נראה
  8 ג'יגה בגלל שטח שמוזמן מראש.
- **בדיקת מצב:** `.venv\Scripts\python.exe scripts\check_data.py`
- **בדיקות:** `.venv\Scripts\python.exe -m pytest -q` — 372 עוברות
- **ניתוח הרצה בלי GPU:** `scripts\analyse_run.py outputs\eval_...`
- יחידות קולאב: נוצלו כ‑14 מתוך 100.

## כללי ברזל

1. **אף נתיב קשיח תחת `src/`.** יש בדיקה שנכשלת על זה.
2. **הנוטבוק לא מכיל לוגיקה.**
3. **משימה לא הושלמה בלי ראיה** — פלט אמיתי מהרצה, לא הצהרה.
4. **אין נשירה שקטה.** כל דגימה שנזרקת נספרת עם סיבה.
5. **`ruff check` נקי ו‑`pytest` ירוק לפני כל commit.**
6. **commit בסוף כל משימה מאומתת.**
7. **לעדכן את `PROGRESS.md`** בסוף כל משימה.
8. **כל עריכת קוד מאומתת אחריה.** בסשן הקודם שלוש החלפות מחרוזת נכשלו בשקט
   ועברו `ruff` ובדיקות, כי הקוד היה תקין — פשוט לא השתנה.
