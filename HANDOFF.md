# העבר את זה לשיחה החדשה

העתק את כל מה שמתחת לקו, והדבק כהודעה ראשונה בשיחה החדשה.

פתח את השיחה **בתיקייה** `C:\Users\omrit\Desktop\nib` — שם `CLAUDE.md` נטען
אוטומטית וחוסך את רוב ההסבר.

---

אנחנו ממשיכים פרויקט שכבר רץ. התיקייה היא `C:\Users\omrit\Desktop\nib`.

**קרא קודם את `CLAUDE.md`, אחריו את הקובץ הזה (`HANDOFF.md`), ואחריו את
`PROGRESS.md`.** הם מחזיקים את הסכם העבודה, מצב כל המשימות והמשימה הבאה. אל
תשאל אותי מה כבר עשינו — זה כתוב שם.

## מי אני ואיך לעבוד איתי

אני עמרי, סטודנט שנה שלישית להנדסת נתונים בטכניון. אני הארכיטקט: אני מחליט,
מריץ ומדבג. אתה כותב את רוב הקוד.

- **ענה בעברית.** מונחים טכניים, שמות מודלים וקבצים נשארים באנגלית, בגרשיים
  אחוריים. אל תשלב אנגלית באמצע משפט עברי בלי סימון.
- **דבר על עצמך בלשון זכר** ("אני בודק", "אני יכול"), לא בלשון נקבה.
- **הקוד באנגלית** — שמות, הערות, תיעוד.
- **הסבר לפני קוד**, ורכיב אחד בכל פעם. **פרק ז'רגון** — הבסיס שלי מוצק
  (סטטיסטיקה, אלגברה, פייתון), אוצר המילים של deep learning פחות.
- **אל תיגע בקבצים מחוץ לתיקייה** בלי אישור לפעולה הספציפית.
- **אל תיתן הערכות זמן שלא מדדת.** מותר חשבון על קצב שנמדד — ותגיד שזה חשבון.
- **בדוק לפני שאתה מחכה** — זמן מעבד מול זמן שעון.
- **אני מריץ בקולאב ומדביק לך פלט.** תגיד לי בדיוק איזה תא להריץ ומה לשלוח.

## מה הפרויקט והמטרה

`nib` — סינתזת כתב יד. **המטרה (עמרי, 14.9): מערכת שעובדת** — מצלמים עמוד של
כתב היד שלך, מקלידים טקסט, ומקבלים אותו בכתב היד שלך. לא תיק עבודות ולא מאמר.
גישות חדשות בסדר, GPU לא מגבלה. **אנגלית בלבד.** כוונון קצר לכל משתמש מותר אם
הוא בזמן סביר.

## המצב, בקצרה

המודל הוא **Emuru** (מוכן, zero-shot). המדד הראשי הוא **זהות כותב לפי `HWD`**:
כמה מהזהות של שורות אמיתיות הפלט נושא (0% = כתב יד של אף אחד, 100% = כמו השורות
האמיתיות). כל ריצה מודדת את הרצפה והתקרה של עצמה.

```
                                     זהות HWD      CER (אמיתי ~11%)
Emuru, ניסיון אחד (תא 6)             56.5%         30.4%
+ בקרת איכות, הקריא הראשון (7c)      65.0%         12.5%     ← ברירת המחדל
+ הכי דומה ליד (7e, 150 שורות)       69.8%         12.0%     ← אופציה, ~פי 3 זמן
```

- **7e מול 7c, שורה מול שורה:** +5.2 נקודות [-0.5, 10.1] — כנראה אמיתי, לא מוכח.
- **כוונון לכל כותב:** 7d לא שיפר (+0.1 [-6.8, 6.7]); 7f, עם הכתב מוסתר, **החמיר**
  (-17.9 [-24.7, -11.1]). **נסגר** בהגדרות האלה.
- **שתי שורות סגנון זו לצד זו:** החמירו הכל. נסגר.
- **חיתוך צמוד של הפלט:** האותיות "קטנות" רק כי Emuru משאיר 7 שורות לבנות מעל
  ומתחת; חיתוך צמוד הוריד identity ב‑4.7 [-7.2, -2.3]. נסגר.
- **פיצול עמוד מצולם לשורות:** עובד. העמוד של עמרי (`data/raw/personal/passage_page1.jpg`,
  עט על דף שורות) → 22/22 שורות. **יש רק עמוד אחד**; עמרי לא כותב עמוד שני.
- **7g, הכתב של עמרי:** 22/22 שורות, קריא כמו הכתב שלו. לדבריו "הבסיס לא רע בכלל"
  אבל עוד לא הכתב שלו — **חלקים קטנים נעלמים** (נקודות, נקודתיים, #, קטעי אותיות).
  נבדק ונשלל: לא ה‑VAE, לא דהייה כללית, לא תפרים בין פרוסות. כנראה המודל עצמו
  (רגרסיה שממצעת סימנים קטנים) — הסקה, לא מדידה.
- **תוקן (16.9):** T34 — החיתוך בין שורת הסגנון לפלט; T35 — ניסיון שכותב מעבר
  לטקסט נפסל. בדיקה ויזואלית: הזבל בסוף שורה נעלם, סימנים בתחילת שורה 7 → 2.

## המשימה הבאה — DiffBrush על T4

עמרי בחר מודל חלופי: **DiffBrush** (ICCV 2025, מודל diffusion שכותב שורה שלמה, MIT). הסקירה ב‑
`docs/research-2026-09-17-alternative-models.md`. תוכנית עם נקודות עצירה:
(1) עטיפה ובדיקה על CPU — **נעשה**; (2) בלי כוונון על 150 שורות CVL — מספר בלבד;
(3) כוונון LoRA לכל כותב על 24 הכותבים של 7d — **חייב לעקוף את Emuru מוצמד**;
(4) העמוד של עמרי.

הקוד מוכן ונבדק על CPU (T36, T37). במחברת: **7h** (הקמה: clone של DiffBrush ב‑`da9addc`,
משקולות 1.17 GB נשמרות ב‑Drive), **7i** (שלב 2), **7j** (העמוד של עמרי בלי כוונון),
**7k** (שלב 3). הסדר המומלץ: 1–4, 7h, **7k**, ואז 7j ו‑7i. הקריטריון של 7k כתוב בתא:
`fine-tuned minus Emuru released` כולו מעל אפס, ו‑Emuru עצמו יוצא קרוב ל‑53.7% של 7d.

- DiffBrush לא נוגע ב‑Emuru: `--generator diffbrush` רק מוסיף אפשרות.
- הקוד שלו ב‑`third_party/` (לא נכנס ל‑git), המשקולות ב‑`checkpoints/diffbrush/`.
- אם 7k נכשל — DiffBrush נסגר בהגדרות האלה; הבא בתור בסקירה: מודל הפסקאות (רישיון לא ידוע).

פתוח עדיין: מבחן עיוור עם אנשים שמכירים את הכתב, והרכבת עמוד שלם (פריסה) למוצר —
נדחה עד שיהיה מודל שכותב שורות כמו שצריך.

## דברים מעשיים

- הקוד ב‑`github.com/omritzabari/nib`, ציבורי. **לדחוף אחרי שינוי קוד** — קולאב
  מושך מ‑GitHub.
- **הנוטבוק לא מכיל לוגיקה.** לעריכת תאים — סקריפט פייתון שמעגן לפי תוכן התא,
  לא `NotebookEdit` (הוא ממספר תאים לפי מיקום ושבר את המחברת פעם אחת).
- ב‑Drive: `MyDrive/nib/cvl_lines_64.lmdb` (127 מגה), `checkpoints/writer_embedder.pt`,
  `personal/`, ותוצאות ב‑`results/`.
- **`hwd` לא נטען מקומית** (אין `editdistance` ל‑Python 3.13 על Windows). הרצות
  מקומיות עוברות דרך סקריפט עם stub שזורק אם נקרא, **ועם `num_workers=0`** — אחרת
  ה‑DataLoader של `hwd` נתקע ב‑Windows. מקומית הוא משחזר את מספרי קולאב בדיוק.
- **בדיקות:** `.venv\Scripts\python.exe -m pytest` — 481 עוברות, 5 מדולגות, 1 xfail.
- **ניתוח בלי GPU:** `scripts\analyse_run.py outputs\eval_...`; תיקיות ריצה מורדות
  מ‑Drive לתוך `outputs/`.

## כללי ברזל

1. **אף נתיב קשיח תחת `src/`.**
2. **הנוטבוק לא מכיל לוגיקה.**
3. **משימה לא הושלמה בלי ראיה** — פלט אמיתי מהרצה, לא הצהרה.
4. **אין נשירה שקטה.** כל דגימה שנזרקת נספרת עם סיבה.
5. **`ruff check`, `ruff format --check` ו‑`pytest` ירוקים לפני כל commit.**
6. **commit בסוף כל משימה מאומתת**, ו‑`PROGRESS.md` מעודכן.
7. **הבוחר לא יכול להיות השופט:** מה שבוחר בין ניסיונות לא מודד את התוצאה.
8. **השוואה בין שיטות — באותן שורות, מול אותו ייחוס, בהפרש לפי כותב.**
