# העבר את זה לשיחה החדשה

העתק את כל מה שמתחת לקו, והדבק כהודעה ראשונה בשיחה החדשה.

חשוב: פתח את השיחה החדשה **בתיקייה** `C:\Users\omrit\Desktop\nib` — לא בתיקייה
שעבדנו בה עד היום. שם `CLAUDE.md` נטען אוטומטית וחוסך את רוב ההסבר.

---

אנחנו ממשיכים פרויקט שכבר רץ. התיקייה היא `C:\Users\omrit\Desktop\nib`.

**קרא קודם את `CLAUDE.md` ואת `PROGRESS.md` שבשורש התיקייה.** הם מחזיקים את
הסכם העבודה, את מצב כל המשימות ואת המשימה הבאה. הם מתוחזקים בדיוק בשביל הרגע
הזה — אל תשאל אותי מה כבר עשינו, זה כתוב שם.

## מי אני ואיך לעבוד איתי

אני עמרי, סטודנט שנה שלישית להנדסת נתונים בטכניון. אני הארכיטקט: אני מחליט,
מריץ ומדבג. אתה כותב את רוב הקוד.

- **ענה בעברית.** מונחים טכניים, שמות מודלים וקבצים נשארים באנגלית.
- **הקוד באנגלית** — שמות, הערות, תיעוד. הרפוזיטורי צריך להיקרא טוב למראיין.
- **הסבר לפני קוד**, ורכיב אחד בכל פעם.
- **פרק ז'רגון.** הבסיס שלי מוצק — הסתברות, סטטיסטיקה, אלגברה, בסיסי נתונים,
  פייתון. אוצר המילים של deep learning ו‑computer vision פחות. הגדר כל מונח
  בשורה אחת בפעם הראשונה.
- **אל תיגע בקבצים מחוץ לתיקיית הפרויקט** בלי אישור לפעולה הספציפית.

## מה הפרויקט

`nib` — סינתזת כתב יד few-shot. מקבל עמוד־שניים של כתב יד, מחלץ את הסגנון,
ומייצר טקסט חדש באותו כתב. **אנגלית בלבד** — עברית ירדה מהתוכנית ואני אודיע אם
היא חוזרת. המטרה: פרויקט מרכזי בתיק העבודות לראיונות דאטא סיינס.

## איפה אנחנו

**שלב 1 הושלם — 13 מתוך 13 משימות.** כל התשתית בנויה ונבדקה על חומרה אמיתית:
נתונים ארוזים, פיצול לפי כותב, שמירה וחידוש מדויקים, מעקב ניסויים, שלושה מדדים
מאומתים, ולולאת קולאב שעובדת.

**שלב 2 התחיל, והמודל כבר מייצר כתב יד.** ב‑31 באוגוסט הפרויקט ייצר לראשונה
כתב יד של כותב שהמערכת לא ראתה מעולם, בסגנון תואם.

## ארבעה מספרים שכל השאר נמדד מולם

אלה מה ש**כתב יד אמיתי** מקבל. הם התקרה, לא היעד. נמדדו על **שורות**,
ושמורים ב־`references/references_cvl_lines_64.json`:

```
FID              19.06     שני חצאים של שורות אמיתיות
CER              13.73%    שיעור הטעות של מודל הקריאה על שורות אמיתיות
זיהוי כותבים     83.7%     top-1 · 97.8% top-5
חידוש אחרי ניתוק  0.000e+00 זהה ביט־לביט, אומת על T4
```

המספרים המקבילים על **מילים** — ‏FID 33.72 וזיהוי 66.9% — הם של שלב 1 ואינם
בסיס תקף להשוואה מול שורות מיוצרות. הפירוט ב־`PROGRESS.md`.

## החלטות נעולות

- **מתחילים ממודל מוכן, לא מאמנים מאפס.** סקר ספרות הראה שהארכיטקטורה שתכננתי
  במקור עוקפה פעמיים. משתמשים ב‑Emuru (CVPR 2025), zero-shot, רישיון MIT.
- **64 פיקסל.** כל הצ'קפוינטים הקיימים בעולם עובדים ב‑32 או 64.
- **CVL ולא IAM** — אתר IAM נפל ולא חזר. CVL זמין בלי הרשמה ונבנה לזיהוי כותבים.
- **`transformers` נעול ל‑4.x.** גרסה 5 לא טוענת את Emuru ושוברת את ה‑tokenizer
  של TrOCR — אותו פער בדיוק.
- **torch נעול ל‑2.11** כדי להתאים לקולאב.

## מה עובד ומה לא

**עובד:** כל שכבת הנתונים, שלושת המדדים, החידוש המדויק, ייצור ברמת שורה,
חבילת השורות (`cvl_lines_64.lmdb`, ‏10,862 שורות), והרפרנסים ברמת שורה.

**לא עובד / פתוח:**
- **ייצור ברמת מילה נכשל** — `normalise_word` מותח כל מילה לגובה קבוע, ומילה בת
  אות אחת נעשית בגובה שורה. מידע הסקאלה נהרס. **חייבים לעבוד ברמת שורה.**
- **Emuru דורש את התמלול של דוגמת הסגנון.** משתמש שמצלם דף לא תימלל אותו, אז
  המערכת תצטרך לקרוא אותו קודם. מגבלת מוצר אמיתית.
- **ייצור על CPU איטי** — 220 שניות לשורה. הייצור שייך לקולאב. המדידה לא:
  ‏`check_metrics.py` רץ מקומית בעשרים דקות.

**נסגר ב־31 באוגוסט:** "לפעמים הוא לא עוצר" לא היה כשל של המודל אלא תקציב
טוקנים שלנו שנקבע עבור מילים. טוקן אחד הוא 8 פיקסלים, נתנו 96 — כלומר 768
פיקסל, מול שורה ממוצעת של 886. התקציב עכשיו נגזר מאורך הטקסט, וכל קטיעה נספרת.

## המשימה הבאה

**להריץ את `notebooks/colab_eval.ipynb`.** צריך להעלות קודם את
`data/processed/upload/cvl_lines_64.lmdb` ל־`MyDrive/nib/` — את העותק מ־`upload/`
בלבד, לא את זה שב־`data/processed/` שנראה 8 ג'יגה. הפירוט ב־`PROGRESS.md`.

## דברים מעשיים

- הקוד ב‑`github.com/omritzabari/nib`, ציבורי.
- שני נוטבוקים, שניהם מכילים **אפס לוגיקה** — רק קוראים לסקריפטים:
  `notebooks/colab_smoke.ipynb` (תשתית, שלב 1) ו־`notebooks/colab_eval.ipynb`
  (הערכת הגנרטור, שלב 2).
- הנתונים ב‑Drive: `MyDrive/nib/cvl_words_64.lmdb` ו‑`cvl_lines_64.lmdb`.
- **בדיקת מצב:** `.venv\Scripts\python.exe scripts\check_data.py`
- **הבדיקות:** `.venv\Scripts\python.exe -m pytest -q` — כ‑325 בדיקות
- **לוח המשימות:** https://claude.ai/code/artifact/01c251cf-a69b-4c24-8dc8-c6e7d72775af
  (אפשר לעדכן אותו — העבר את ה‑URL כפרמטר `url`)

## כללי ברזל של הפרויקט

1. **אף קובץ תחת `src/` לא מכיל נתיב קשיח.** יש בדיקה שנכשלת על זה.
2. **הנוטבוק לא מכיל לוגיקה.**
3. **משימה לא "הושלמה" בלי ראיה** — פלט אמיתי מהרצה, לא הצהרה.
4. **אין נשירה שקטה.** כל דגימה שנזרקת נספרת עם סיבה.
5. **`ruff check` נקי ו‑`pytest` ירוק לפני כל commit.**
6. **commit בסוף כל משימה מאומתת**, עם הודעה שמתעדת מה השתנה ולמה.
7. **לעדכן את `PROGRESS.md`** בסוף כל משימה — זה מה שמאפשר את ההעברה הזו.
